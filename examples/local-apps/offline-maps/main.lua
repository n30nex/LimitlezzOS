-- title: Offline Maps
-- status: Map tiles unavailable in SDK 0.1
-- body: This shell proves maps can live outside the base firmware image.
-- action: Center | Map centered #{count} | Centered the local map shell {count} times. | counter:center
return true
