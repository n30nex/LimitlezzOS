-- title: Field Notes
-- status: Local notes sandbox
-- body: Notes will stay in scoped app storage once text editing APIs land.
-- action: New note | Note #{count} staged | Created {count} placeholder notes in scoped app data. | counter:notes
-- action: Pin | Pin #{count} staged | Pinned {count} note markers for this app only. | counter:pins
return true
