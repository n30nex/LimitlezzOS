-- title: Mesh BBS
-- status: Local bulletin board shell
-- body: Public posts stay inside the app sandbox until mesh APIs are injected.
-- action: Sync | BBS sync #{count} | Checked the bulletin board shell {count} times. | counter:syncs
return true
