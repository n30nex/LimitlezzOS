-- title: LoRa Chess
-- status: Waiting for a local board
-- body: Turn state stays scoped to this app before mesh move routing is added.
-- action: New game | Game #{count} staged | Created {count} local chess boards. | counter:games
-- action: Move | Move #{count} staged | Recorded {count} local moves in scoped storage. | counter:moves
return true
