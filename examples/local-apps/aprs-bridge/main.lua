-- title: APRS Bridge
-- status: Bridge disabled in SDK 0.1
-- body: This shell keeps APRS and Wi-Fi bridge work out of the base OS image.
-- action: Queue | Packet #{count} queued | Queued {count} placeholder APRS packets locally. | counter:packets
return true
