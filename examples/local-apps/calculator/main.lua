-- title: Calculator
-- status: SDK 0.1 foreground sandbox
-- body: A tiny keypad shell for field math. Runtime arithmetic lands after the VM choice.
-- action: Clear | Calculator cleared | The display state was reset inside the foreground shell.
return true
